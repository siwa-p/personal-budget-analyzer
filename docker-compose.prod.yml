services:
  backend:
    image: ${ECR_BASE}/budget-analyzer/backend:latest
    container_name: budget_analyzer_backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env.prod
    volumes:
      - hf_cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  hf_cache:
